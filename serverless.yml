service:
  name: product-service

app: 'zoco-de-oro-rest-api'

plugins:
  - serverless-bundle
  - serverless-pseudo-parameters

provider:
  stage: ${opt:stage}
  name: aws
  runtime: nodejs12.x
  memorySize: 256
  region: ${opt:region, 'us-east-2'}
  apiGateway:
    restApiId: 8cjvfmx3qk # REST API resource ID. Default is generated by the framework
    restApiRootResourceId: 1abdgoc1h4 # Root resource ID, represent as / path

functions:
  getProductList:
    handler: src/handlers/getProductList.handler
    name: getProductList-lambda-${opt:stage} # optional, Deployed Lambda name
    events:
      - http:
          method: GET
          path: /products
  getProductDetail:
    handler: src/handlers/getProductDetail.handler
    name: getProductDetail-lambda-${opt:stage} # optional, Deployed Lambda name
    events:
      - http:
          method: GET
          path: /products/{ID}
custom:
  bundle:
    linting: false
